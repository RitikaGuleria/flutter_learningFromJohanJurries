import 'package:flutter/material.dart';

class Default{

  static final Color? draweritemColor = Colors.blueGrey[700];
  static final Color? draweritemSelecetdColor = Colors.blue[500];
  static final Color? tileColor = Colors.blue[100];

  static final drawerItemText= [
    'Inbox','Starred','Sent','Drafts','Trash','Spam',
  ];

  static final drawerItemIcon= [
    Icons.inbox,
    Icons.star,
    Icons.send,
    Icons.mail,
    Icons.delete,
    Icons.warning_rounded,
  ];
}